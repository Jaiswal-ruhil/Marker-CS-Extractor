name: Build Windows Executable

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build Windows executable
        shell: cmd
        run: |
          pyinstaller --onefile --name "MarkerCSExtractor" --hidden-import pandas --hidden-import openpyxl --hidden-import openpyxl.styles --hidden-import openpyxl.utils --noconsole main.py

      - name: Verify build
        shell: cmd
        run: dir dist\

      - name: Upload Windows executable
        uses: actions/upload-artifact@v4
        with:
          name: MarkerCSExtractor-Windows
          path: dist\MarkerCSExtractor.exe
          retention-days: 30

      - name: Create Release (on push to main)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: "Windows Build #${{ github.run_number }}"
          body: |
            Automated Windows build from commit ${{ github.sha }}

            **How to use:**
            1. Download `MarkerCSExtractor.exe`
            2. Double-click it, or run from Command Prompt:
               `MarkerCSExtractor.exe invoice.pdf`
            Or drag-and-drop PDF files onto the executable in File Explorer.
          files: dist\MarkerCSExtractor.exe
          draft: false
          prerelease: false